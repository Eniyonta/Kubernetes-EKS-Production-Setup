cat <<EOF > .gitignore
# Secrets & credentials
*.pem
*.key
*.env
.env*
kubeconfig
*kubeconfig*

# Terraform state (contains secrets!)
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Helm cache
charts/
EOF
